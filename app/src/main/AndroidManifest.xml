<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.popdate"> <!-- 패키지 이름 추가 -->

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".GlobalApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Popdate"
        tools:targetApi="31"
        >

        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="@string/kakao_app_key" />

        <!-- 카카오 로그인, 인가코드를 받기 위한 액티비티 -->
        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                 <!-- Redirect URI: "kakao${NATIVE_APP_KEY}://oauth" -->
                <data android:host="oauth"
                      android:scheme="kakao758d531abf8bd788cdb0308888945ba9" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".MyPageActivity">
            <!-- 이 액티비티를 사용하는 인텐트 필터가 있을 경우 여기에 추가 -->
        </activity>

        <activity android:name=".FavoritesActivity">
            <!-- 이 액티비티를 사용하는 인텐트 필터가 있을 경우 여기에 추가 -->
        </activity>

        <activity android:name=".UserInfoActivity">
            <!-- 이 액티비티를 사용하는 인텐트 필터가 있을 경우 여기에 추가 -->
        </activity>

        <!-- 다른 활동이나 서비스가 여기에 추가될 수 있습니다 -->

    </application>

</manifest>
